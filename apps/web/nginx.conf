server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Clean URL for the standalone donate page
  location = /donate {
    try_files /donate.html =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
